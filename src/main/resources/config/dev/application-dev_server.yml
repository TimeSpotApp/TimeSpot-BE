server:
  address: ${SERVER_ADDRESS:localhost}
  port: ${SERVER_PORT:8080}
  max-http-request-header-size: 8KB
  servlet:
    context-path: /
    encoding:
      enabled: true
      charset: UTF-8
      force: true
    session:
      #timeout: 30m
      persistent: false
  error:
    path: /error
  tomcat:
    accept-count: 100
    max-connections: 8192
    max-http-form-post-size: 2MB
    max-swallow-size: 2MB
    uri-encoding: UTF-8
    threads:
      max: 200
      min-spare: 10
  shutdown: graceful
spring:
  devtools:
    livereload:
      enabled: true
  lifecycle:
    timeout-per-shutdown-phase: 30s
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 0B
      max-file-size: 1MB
      max-request-size: 10MB
  jackson:
    property-naming-strategy: LOWER_CAMEL_CASE
  task:
    execution:
      pool:
        allow-core-thread-timeout: true
        core-size: 8
        keep-alive: 60s
        #max-size:
        #queue-capacity:
      thread-name-prefix: task-
      shutdown:
        await-termination: false
        #await-termination-period:
    scheduling:
      pool:
        size: 1
      thread-name-prefix: scheduling-
      shutdown:
        await-termination: false
        #await-termination-period:
app:
  rate-limit:
    default:
      capacity: ${RATE_LIMIT_CAPACITY_DEFAULT:100}
      duration-minutes: ${RATE_LIMIT_DURATION_MINUTES_DEFAULT:1}
      refill-strategy: greedy
    anonymous:
      capacity: ${RATE_LIMIT_CAPACITY_ANONYMOUS:20}
      duration-minutes: ${RATE_LIMIT_DURATION_MINUTES_ANONYMOUS:1}
      refill-strategy: greedy
    authenticated:
      capacity: ${RATE_LIMIT_CAPACITY_AUTHENTICATED:300}
      duration-minutes: ${RATE_LIMIT_DURATION_MINUTES_AUTHENTICATED:1}
      refill-strategy: greedy
    endpoints:
      - path-pattern: "/api/v1/auth/login"
        capacity: ${RATE_LIMIT_CAPACITY_AUTH_LOGIN:5}
        duration-minutes: ${RATE_LIMIT_DURATION_MINUTES_AUTH_LOGIN:1}
      - path-pattern: "/api/v1/auth/refresh-token"
        capacity: ${RATE_LIMIT_CAPACITY_AUTH_REFRESH_TOKEN:10}
        duration-minutes: ${RATE_LIMIT_DURATION_MINUTES_AUTH_REFRESH_TOKEN:1}
      - path-pattern: "/api/v1/users"
        capacity: ${RATE_LIMIT_CAPACITY_USERS:60}
        duration-minutes: ${RATE_LIMIT_DURATION_MINUTES_USERS:1}
  api:
    visit-korea:
      service-key: ${VISIT_KOREA_SERVICE_KEY:62166eff31104a8a966eff3110ca8a22}
      base-url: ${VISIT_KOREA_BASE_URL:https://apis.data.go.kr/B551011/KorService2}
      max-radius-meters: ${PLACE_MAX_RADIUS_METERS:5000}
      default-radius-meters: ${PLACE_DEFAULT_RADIUS_METERS:1500}