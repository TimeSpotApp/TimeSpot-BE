server:
  address: ${SERVER_ADDRESS:0.0.0.0}
  port: ${SERVER_PORT:8080}
  forward-headers-strategy: framework
  max-http-request-header-size: 8KB
  servlet:
    context-path: /
    encoding:
      enabled: true
      charset: UTF-8
      force: true
    session:
      #timeout: 30m
      persistent: false
  error:
    path: /error
  tomcat:
    accept-count: 100
    max-connections: 8192
    max-http-form-post-size: 2MB
    max-swallow-size: 2MB
    uri-encoding: UTF-8
    threads:
      max: 200
      min-spare: 10
    mbeanregistry:
      enabled: true
  shutdown: graceful
spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 0B
      max-file-size: 1MB
      max-request-size: 10MB
  jackson:
    property-naming-strategy: LOWER_CAMEL_CASE
  task:
    execution:
      pool:
        allow-core-thread-timeout: true
        core-size: 8
        keep-alive: 60s
        #max-size:
        #queue-capacity:
      thread-name-prefix: task-
      shutdown:
        await-termination: false
        #await-termination-period:
    scheduling:
      pool:
        size: 1
      thread-name-prefix: scheduling-
      shutdown:
        await-termination: false
        #await-termination-period:
app:
  rate-limit:
    default:
      capacity: ${RATE_LIMIT_CAPACITY_DEFAULT}
      duration-minutes: ${RATE_LIMIT_DURATION_MINUTES_DEFAULT}
      refill-strategy: greedy
    anonymous:
      capacity: ${RATE_LIMIT_CAPACITY_ANONYMOUS}
      duration-minutes: ${RATE_LIMIT_DURATION_MINUTES_ANONYMOUS}
      refill-strategy: greedy
    authenticated:
      capacity: ${RATE_LIMIT_CAPACITY_AUTHENTICATED}
      duration-minutes: ${RATE_LIMIT_DURATION_MINUTES_AUTHENTICATED}
      refill-strategy: greedy
    endpoints:
      - path-pattern: "/api/v1/auth/login"
        capacity: ${RATE_LIMIT_CAPACITY_AUTH_LOGIN:5}
        duration-minutes: ${RATE_LIMIT_DURATION_MINUTES_AUTH_LOGIN:1}
      - path-pattern: "/api/v1/auth/refresh-token"
        capacity: ${RATE_LIMIT_CAPACITY_AUTH_REFRESH_TOKEN:10}
        duration-minutes: ${RATE_LIMIT_DURATION_MINUTES_AUTH_REFRESH_TOKEN:1}
      - path-pattern: "/api/v1/users"
        capacity: ${RATE_LIMIT_CAPACITY_USERS:60}
        duration-minutes: ${RATE_LIMIT_DURATION_MINUTES_USERS:1}
  api:
    visit-korea:
      service-key: ${VISIT_KOREA_SERVICE_KEY}
      base-url: ${VISIT_KOREA_BASE_URL}
      max-radius-meters: ${PLACE_MAX_RADIUS_METERS}
      default-radius-meters: ${PLACE_DEFAULT_RADIUS_METERS}